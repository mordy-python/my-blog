run = [
    "deno",
    "task",
    "run"
]

hidden = [".deno"]

entrypoint = "index.ts"

[env]
DENO_DIR = "/home/runner/${REPL_SLUG}/.deno"
PATH = "$DENO_DIR/bin:PATH"

[nix]
channel = "stable-21_11"